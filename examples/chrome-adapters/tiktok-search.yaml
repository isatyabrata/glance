# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/tiktok/search.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/tiktok-search.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: tiktok
name: search
description: Search TikTok videos
domain: www.tiktok.com

args:
  query:
    type: str
    required: true
    positional: true
    description: Search query
  limit:
    type: int
    default: 10
    description: Number of results

pipeline:
  - navigate:
      url: https://www.tiktok.com/explore
      settleMs: 5000

  - evaluate: |
      (async () => {
        const query = ${{ args.query | json }};
        const limit = ${{ args.limit }};
        const res = await fetch('/api/search/general/full/?keyword=' + encodeURIComponent(query) + '&offset=0&count=' + limit + '&aid=1988', { credentials: 'include' });
        if (!res.ok) throw new Error('Search failed: HTTP ' + res.status);
        const data = await res.json();
        const items = (data.data || []).filter(function(i) { return i.type === 1 && i.item; });
        return items.slice(0, limit).map(function(i, idx) {
          var v = i.item;
          var a = v.author || {};
          var s = v.stats || {};
          return {
            rank: idx + 1,
            desc: (v.desc || '').replace(/\n/g, ' ').substring(0, 100),
            author: a.uniqueId || '',
            plays: s.playCount || 0,
            likes: s.diggCount || 0,
            comments: s.commentCount || 0,
            shares: s.shareCount || 0,
          };
        });
      })()

columns: [rank, desc, author, plays, likes, comments, shares]
