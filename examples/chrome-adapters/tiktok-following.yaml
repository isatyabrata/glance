# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/tiktok/following.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/tiktok-following.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: tiktok
name: following
description: List accounts you follow on TikTok
domain: www.tiktok.com

args:
  limit:
    type: int
    default: 20
    description: Number of accounts

pipeline:
  - navigate:
      url: https://www.tiktok.com/following
      settleMs: 5000

  - evaluate: |
      (() => {
        const limit = ${{ args.limit }};
        const links = Array.from(document.querySelectorAll('a[href*="/@"]'))
          .filter(function(a) {
            const text = a.textContent.trim();
            return text.length > 1 && text.length < 80 &&
              !text.includes('Profile') && !text.includes('More') && !text.includes('Upload');
          });

        const seen = {};
        const results = [];
        for (const a of links) {
          const match = a.href.match(/@([^/]+)/);
          const username = match ? match[1] : '';
          if (!username || seen[username]) continue;
          seen[username] = true;
          const raw = a.textContent.trim();
          const name = raw.replace(username, '').replace('@', '').trim();
          results.push({
            index: results.length + 1,
            username: username,
            name: name || username,
          });
          if (results.length >= limit) break;
        }
        return results;
      })()

columns: [index, username, name]
