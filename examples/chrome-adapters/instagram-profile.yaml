# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/instagram/profile.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/instagram-profile.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: instagram
name: profile
description: Get Instagram user profile info
domain: www.instagram.com

args:
  username:
    type: str
    required: true
    positional: true
    description: Instagram username

pipeline:
  - navigate: https://www.instagram.com

  - evaluate: |
      (async () => {
        const username = ${{ args.username | json }};
        const res = await fetch(
          'https://www.instagram.com/api/v1/users/web_profile_info/?username=' + encodeURIComponent(username),
          {
            credentials: 'include',
            headers: { 'X-IG-App-ID': '936619743392459' }
          }
        );
        if (!res.ok) throw new Error('HTTP ' + res.status + ' - make sure you are logged in to Instagram');
        const data = await res.json();
        const u = data?.data?.user;
        if (!u) throw new Error('User not found: ' + username);
        return [{
          username: u.username,
          name: u.full_name || '',
          bio: (u.biography || '').replace(/\n/g, ' ').substring(0, 120),
          followers: u.edge_followed_by?.count ?? 0,
          following: u.edge_follow?.count ?? 0,
          posts: u.edge_owner_to_timeline_media?.count ?? 0,
          verified: u.is_verified ? 'Yes' : 'No',
          url: 'https://www.instagram.com/' + u.username,
        }];
      })()

columns: [username, name, followers, following, posts, verified, bio]
