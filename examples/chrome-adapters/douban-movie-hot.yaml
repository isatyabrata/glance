# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/douban/movie-hot.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# This file is kept in upstream opencli/autocli format on purpose; running
#   glance chrome import examples/chrome-adapters/douban-movie-hot.yaml
# translates it into glance's schema and writes it under ~/.glance/chrome-adapters/.
# The translator only handles the browser DOM-scrape subset; see README for limits.

site: douban
name: movie-hot
description: 豆瓣电影热门榜单 (DOM scrape)
domain: movie.douban.com
strategy: cookie
browser: true

args:
  limit:
    type: int
    default: 20
    description: 返回的电影数量

pipeline:
  - navigate: https://movie.douban.com/chart

  - evaluate: |
      (async () => {
        await new Promise(r => setTimeout(r, 4000));
        const normalize = (value) => (value || '').replace(/\s+/g, ' ').trim();
        const limit = Math.max(1, Math.min(${{ args.limit }} || 20, 50));
        const results = [];
        for (const el of Array.from(document.querySelectorAll('.item'))) {
          const titleEl = el.querySelector('.pl2 a');
          const title = normalize(titleEl?.textContent);
          let url = titleEl?.getAttribute('href') || '';
          if (!title || !url) continue;
          if (!url.startsWith('http')) url = 'https://movie.douban.com' + url;

          const info = normalize(el.querySelector('.pl2 p')?.textContent);
          const infoParts = info.split('/').map((part) => part.trim()).filter(Boolean);
          const releaseIndex = (() => {
            for (let i = infoParts.length - 1; i >= 0; i -= 1) {
              if (/\d{4}-\d{2}-\d{2}|\d{4}\/\d{2}\/\d{2}/.test(infoParts[i])) return i;
            }
            return -1;
          })();
          const directorPart = releaseIndex >= 1 ? infoParts[releaseIndex - 1] : '';
          const regionPart = releaseIndex >= 2 ? infoParts[releaseIndex - 2] : '';
          const yearMatch = info.match(/\b(19|20)\d{2}\b/);
          results.push({
            rank: results.length + 1,
            title,
            rating: parseFloat(normalize(el.querySelector('.rating_nums')?.textContent)) || 0,
            quote: normalize(el.querySelector('.inq')?.textContent),
            director: directorPart.replace(/^导演:\s*/, ''),
            year: yearMatch?.[0] || '',
            region: regionPart,
            url,
          });
          if (results.length >= limit) break;
        }
        return results;
      })()

columns: [rank, title, rating, quote, director, year, region, url]
