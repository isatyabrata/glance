# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/boss/joblist.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/boss-joblist.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: boss
name: joblist
description: "BOSS直聘查看我发布的职位列表"
domain: www.zhipin.com
strategy: cookie
browser: true

columns: [job_name, salary, city, status, encrypt_job_id]

pipeline:
  - navigate: https://www.zhipin.com/web/chat/index
  - wait: 2
  - evaluate: |
      (async () => {
        const url = 'https://www.zhipin.com/wapi/zpjob/job/chatted/jobList';
        const resp = await new Promise((resolve, reject) => {
          const xhr = new XMLHttpRequest();
          xhr.open('GET', url, true); xhr.withCredentials = true; xhr.timeout = 15000;
          xhr.setRequestHeader('Accept', 'application/json');
          xhr.onload = () => { try { resolve(JSON.parse(xhr.responseText)); } catch(e) { reject(e); } };
          xhr.onerror = () => reject(new Error('Network Error'));
          xhr.ontimeout = () => reject(new Error('Timeout'));
          xhr.send(null);
        });
        if (resp.code !== 0) return [{ job_name: 'Error', salary: '', city: '', status: resp.message || '', encrypt_job_id: '' }];
        return (resp.zpData || []).map(j => ({
          job_name: j.jobName || '', salary: j.salaryDesc || '', city: j.address || '',
          status: j.jobOnlineStatus === 1 ? '在线' : '已关闭', encrypt_job_id: j.encryptJobId || '',
        }));
      })()
