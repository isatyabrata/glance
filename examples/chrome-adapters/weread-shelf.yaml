# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/weread/shelf.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/weread-shelf.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: weread
name: shelf
description: List books on your WeRead bookshelf
domain: weread.qq.com
strategy: cookie
browser: true

args:
  limit:
    type: int
    default: 20
    description: Max results

pipeline:
  - navigate:
      url: https://weread.qq.com
      settleMs: 3000

  - evaluate: |
      (async () => {
        const limit = ${{ args.limit }} || 20;
        const res = await fetch('/web/shelf/sync?synckey=0&lectureSynckey=0', { credentials: 'include' });
        if (!res.ok) throw new Error('HTTP ' + res.status);
        const data = await res.json();
        if (data?.errCode === -2010) throw new Error('Not logged in to WeRead');
        const books = data?.books || [];
        const progress = data?.bookProgress || [];
        const progressMap = {};
        progress.forEach(p => { progressMap[p.bookId] = p.progress; });
        return books.slice(0, limit).map(item => ({
          title: item.title || '',
          author: item.author || '',
          cover: item.cover || '',
          progress: progressMap[item.bookId] != null ? progressMap[item.bookId] + '%' : '-',
          bookId: item.bookId || '',
        }));
      })()

columns: [title, author, cover, progress, bookId]
