# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/xueqiu/watchlist.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/xueqiu-watchlist.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: xueqiu
name: watchlist
description: 获取雪球自选股列表
domain: xueqiu.com
browser: true

args:
  category:
    type: str # using str to prevent parsing issues like 01
    default: "1"
    description: "分类：1=自选(默认) 2=持仓 3=关注"
  limit:
    type: int
    default: 100
    description: 默认 100

pipeline:
  - navigate: https://xueqiu.com
  - evaluate: |
      (async () => {
        const category = parseInt(${{ args.category | json }}) || 1;
        const resp = await fetch(`https://stock.xueqiu.com/v5/stock/portfolio/stock/list.json?size=100&category=${category}&pid=-1`, {credentials: 'include'});
        if (!resp.ok) throw new Error('HTTP ' + resp.status + ' Hint: Not logged in?');
        const d = await resp.json();
        if (!d.data || !d.data.stocks) throw new Error('获取失败，可能未登录');

        return d.data.stocks.map(s => ({
          symbol: s.symbol,
          name: s.name,
          price: s.current,
          change: s.chg,
          changePercent: s.percent != null ? s.percent.toFixed(2) + '%' : null,
          volume: s.volume,
          url: 'https://xueqiu.com/S/' + s.symbol
        }));
      })()

  - map:
      symbol: ${{ item.symbol }}
      name: ${{ item.name }}
      price: ${{ item.price }}
      changePercent: ${{ item.changePercent }}

  - limit: ${{ args.limit }}

columns: [symbol, name, price, changePercent]
