# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/facebook/notifications.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/facebook-notifications.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: facebook
name: notifications
description: Get recent Facebook notifications
domain: www.facebook.com

args:
  limit:
    type: int
    default: 15
    description: Number of notifications

pipeline:
  - navigate:
      url: https://www.facebook.com/notifications
      settleMs: 3000

  - evaluate: |
      (() => {
        const limit = ${{ args.limit }};
        const items = document.querySelectorAll('[role="listitem"]');
        return Array.from(items)
          .filter(el => el.querySelectorAll('a').length > 0)
          .slice(0, limit)
          .map((el, i) => {
            const raw = el.textContent.trim().replace(/\s+/g, ' ');
            // Remove leading "未读" and trailing "标记为已读"
            const cleaned = raw.replace(/^未读/, '').replace(/标记为已读$/, '').replace(/^Unread/, '').replace(/Mark as read$/, '').trim();
            // Try to extract time (last segment like "11小时", "5天", "1周")
            const timeMatch = cleaned.match(/(\d+\s*(?:分钟|小时|天|周|个月|minutes?|hours?|days?|weeks?|months?))\s*$/);
            const time = timeMatch ? timeMatch[1] : '';
            const text = timeMatch ? cleaned.slice(0, -timeMatch[0].length).trim() : cleaned;
            return {
              index: i + 1,
              text: text.substring(0, 150),
              time: time || '-',
            };
          });
      })()

columns: [index, text, time]
