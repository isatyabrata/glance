# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/instagram/saved.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/instagram-saved.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: instagram
name: saved
description: Get your saved Instagram posts
domain: www.instagram.com

args:
  limit:
    type: int
    default: 20
    description: Number of saved posts

pipeline:
  - navigate: https://www.instagram.com

  - evaluate: |
      (async () => {
        const limit = ${{ args.limit }};
        const res = await fetch(
          'https://www.instagram.com/api/v1/feed/saved/posts/',
          {
            credentials: 'include',
            headers: { 'X-IG-App-ID': '936619743392459' }
          }
        );
        if (!res.ok) throw new Error('HTTP ' + res.status + ' - make sure you are logged in to Instagram');
        const data = await res.json();
        return (data?.items || []).slice(0, limit).map((item, i) => {
          const m = item?.media;
          return {
            index: i + 1,
            user: m?.user?.username || '',
            caption: (m?.caption?.text || '').replace(/\n/g, ' ').substring(0, 100),
            likes: m?.like_count ?? 0,
            comments: m?.comment_count ?? 0,
            type: m?.media_type === 1 ? 'photo' : m?.media_type === 2 ? 'video' : 'carousel',
          };
        });
      })()

columns: [index, user, caption, likes, comments, type]
