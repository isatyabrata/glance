# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/xueqiu/hot.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# To install: glance chrome import examples/chrome-adapters/xueqiu-hot.yaml
# (re-imports translate this autocli-format YAML into glance native schema
#  written to ~/.glance/chrome-adapters/.)

# Adapter logic derived from OpenCLI (https://github.com/jackwener/opencli)
# Original author: jackwener | License: Apache-2.0
site: xueqiu
name: hot
description: 获取雪球热门动态
domain: xueqiu.com
browser: true

args:
  limit:
    type: int
    default: 20
    description: 返回数量，默认 20，最大 50

pipeline:
  - navigate: https://xueqiu.com
  - evaluate: |
      (async () => {
        const resp = await fetch('https://xueqiu.com/statuses/hot/listV3.json?source=hot&page=1', {credentials: 'include'});
        if (!resp.ok) throw new Error('HTTP ' + resp.status + ' Hint: Not logged in?');
        const d = await resp.json();
        const list = d.list || [];

        const strip = (html) => (html || '').replace(/<[^>]+>/g, '').replace(/&nbsp;/g, ' ').replace(/&amp;/g, '&').replace(/&lt;/g, '<').replace(/&gt;/g, '>').trim();
        return list.map((item, i) => {
          const user = item.user || {};
          return {
            rank: i + 1,
            text: strip(item.description).substring(0, 200),
            url: 'https://xueqiu.com/' + user.id + '/' + item.id,
            author: user.screen_name,
            likes: item.fav_count,
            retweets: item.retweet_count,
            replies: item.reply_count
          };
        });
      })()

  - map:
      rank: ${{ item.rank }}
      author: ${{ item.author }}
      text: ${{ item.text }}
      likes: ${{ item.likes }}
      url: ${{ item.url }}

  - limit: ${{ args.limit }}

columns: [rank, author, text, likes, url]
