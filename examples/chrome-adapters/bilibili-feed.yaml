# Vendored from autocli (Apache-2.0).
#   Source: https://github.com/nashsu/AutoCLI/blob/c0969e2c83b29a7528452b1ba555085deca8e00d/adapters/bilibili/feed.yaml
#   Adapter logic originally derived from opencli (https://github.com/jackwener/opencli).
#   Original author: jackwener / nash_su.
#
# Requires that you're logged in to bilibili.com in your real Chrome
# profile (the glance bridge reuses the existing session). Run
#   glance chrome import examples/chrome-adapters/bilibili-feed.yaml
# to install.

site: bilibili
name: feed
description: 关注的人的动态时间线 (in-page fetch with the user's session cookies)
domain: www.bilibili.com
strategy: cookie
browser: true

args:
  limit:
    type: int
    default: 20
    description: Number of results
  type:
    default: all
    description: "Filter: all, video, article"

columns: [rank, author, title, type, url]

pipeline:
  - navigate: https://www.bilibili.com

  - evaluate: |
      (async () => {
        const limit = ${{ args.limit }};
        const feedType = '${{ args.type }}';

        function stripHtml(s) {
          return s.replace(/<[^>]+>/g, '').replace(/&[a-z]+;/gi, ' ').trim();
        }

        const typeMap = { all: 'all', video: 'video', article: 'article' };
        const params = new URLSearchParams({
          timezone_offset: '-480',
          type: typeMap[feedType] || 'all',
          page: '1'
        });

        const res = await fetch('https://api.bilibili.com/x/polymer/web-dynamic/v1/feed/all?' + params.toString(), {
          credentials: 'include'
        });
        const payload = await res.json();
        const items = payload?.data?.items || [];
        const rows = [];

        for (let i = 0; i < Math.min(items.length, Number(limit)); i++) {
          const item = items[i];
          const modules = item.modules || {};
          const authorModule = modules.module_author || {};
          const dynamicModule = modules.module_dynamic || {};
          const major = dynamicModule.major || {};

          let title = '';
          let url = '';
          let itemType = item.type || '';

          if (major.archive) {
            title = major.archive.title || '';
            url = major.archive.jump_url ? 'https:' + major.archive.jump_url : '';
            itemType = 'video';
          } else if (major.article) {
            title = major.article.title || '';
            url = major.article.jump_url ? 'https:' + major.article.jump_url : '';
            itemType = 'article';
          } else if (dynamicModule.desc) {
            title = stripHtml(dynamicModule.desc.text || '').slice(0, 60);
            url = item.id_str ? 'https://t.bilibili.com/' + item.id_str : '';
            itemType = 'dynamic';
          }

          if (!title) continue;

          rows.push({
            rank: rows.length + 1,
            author: authorModule.name || '',
            title,
            type: itemType,
            url
          });
        }

        return rows;
      })()
